apiVersion: ecr.aws.upbound.io/v1beta1
kind: Repository
metadata:
  name: buckee-control-plane
spec:
  forProvider:
    region: us-east-1
    imageTagMutability: IMMUTABLE
    forceDelete: true
  providerConfigRef:
    name: default
